# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# pickaxe session data (workspace-specific, lives in the managed repo)
.pickaxe/

# Scratch/POC sandbox — copies used to prototype against without touching
# canonical .HANDOFF/ content (see .AI-TRAINING/mvx-stories rollup discovery)
.sandbox/

# macOS metadata garbage
._*
.DS_Store

# autodoc build artifacts
*.html
*.pdf
